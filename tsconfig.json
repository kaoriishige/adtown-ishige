{
  "compilerOptions": {
    // --- Next.js 推奨の基本設定 ---
    "target": "es5",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true, // Next.jsがコンパイルを行うため、tscはファイルを出力しない
    "module": "esnext", // ES Modules を使用
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    
    // --- エイリアスパス ('@/' の解決) ---
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    },

    // --- ご提示いただいた設定 (維持) ---
    "strict": true,
    "noImplicitReturns": true,
    "noUnusedLocals": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "moduleResolution": "node"
  },
  
  // --- コンパイル対象 ('No inputs' エラーの解決) ---
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx"
, "pages/nasu/kondate.tsx", "pages/nasu/index.tsx"  ],
  "exclude": [
    "node_modules"
  ]
  // "outDir": "lib" は削除します（Next.jsは .next ディレクトリを使用するため）
  // "compileOnSave": true は不要です（'next dev' が自動で行うため）
}

